package common

const ShortDescription = "Generates Terraform configuration files"
const Version = "0.1.0"
const LongDescription = `Build Terraform configuration files from a higher-level config and template directory.`
